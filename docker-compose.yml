version: '3'

services:
  mysql:
    image: mysql:8.0.32
    container_name: mysql_diplom
    ports:
      - "3306:3306"
    environment:
      - MYSQL_DATABASE=app
      - MYSQL_USER=app
      - MYSQL_PASSWORD=pass
      - MYSQL_ROOT_PASSWORD=root